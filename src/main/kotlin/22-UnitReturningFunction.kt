fun main() {
    sayHi()
}
//  Unit returning function is a function that not returning any of data
//  Our previous functions that we made are included in unit return
//  We don't have to add "Unit" keyword because it's default in Unit returning function
//  In other programming language, it's same like void
fun sayHi(): Unit{
    println("Hi my friend!")
}